.zig-cache/
zig-out/
zig-cache/
*.spv

# Upstream TRiP source kept locally for cross-checking; not part of our
# project history (it has its own .git and CC-BY-NC license).
reference/

# Local Claude Code state (memory, scheduled tasks, settings).
.claude/

# Per-run perf bench logs (scripts/bench.sh writes timestamped dirs here).
# The script itself is checked in; the artifacts are local-only.
bench/

# Local research notes / draft specs — kept out of the public repo.
rff/
